// # MCP QA Index
//
// Logline: Index of the six hostile probe drivers. A driver that cannot fail is a defect.
//
// Setup: Each probe returns Result. Ok(0) only after assertions pass.
//
// Beats:
//   1. probe_initialize.oo — initialize, pre-init refuse, duplicate id.
//   2. probe_tools_list.oo — 20 live tools, no ghost names.
//   3. probe_tool_call.oo — schema and unknown tool.
//   4. probe_resources_list.oo — seven URIs, no registry.
//   5. probe_heartbeat.oo — ping before and after Ready.
//   6. probe_double_run.oo — identical initialize on two sessions.

import "probe_initialize.oo";
import "probe_tools_list.oo";
import "probe_tool_call.oo";
import "probe_resources_list.oo";
import "probe_heartbeat.oo";
import "probe_double_run.oo";
